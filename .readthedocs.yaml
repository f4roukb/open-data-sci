version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

mkdocs:
  configuration: libs/open-data-sci/mkdocs.yaml

python:
  install:
    - method: pip
      path: libs/open-data-sci
      extra_requirements:
        - docs
